local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        final aggregation over (i_brand, i_brand_id)
            local exchange (GATHER, SINGLE, [])
                remote exchange (REPARTITION, HASH, ["i_brand", "i_brand_id"])
                    partial aggregation over (i_brand, i_brand_id)
                        join (INNER, REPLICATED):
                            join (INNER, PARTITIONED):
                                dynamic filter (["ss_item_sk", "ss_sold_date_sk"])
                                    scan store_sales
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, local:HivePartitioningHandle{buckets=1, hiveTypes=[], partitions=1824}, ["d_date_sk"])
                                        scan date_dim
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPLICATE, BROADCAST, [])
                                    scan item
